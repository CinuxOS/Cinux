# D1 + D8 + D10 + D12 deterministic 审计报告（Q3 收尾）

> F-QA Q3 收尾。2026-06-21。分支 `feat/f-qa-q2`。Q2 四段式。只读。**Q3 达 14/14 全审。**

## 范围

D1 架构不变量 + D8 测试覆盖（例外：先读码）+ D10 文档/可追溯 + D12 发布/变更管理。元层维度。

## A 锚点命中

### D1

| # | 锚点 | 命中 |
|---|------|------|
| a1 禁用特性 | throw/dynamic_cast/typeid/std::vector 等 — **零命中**（kernel 非 test） |
| a3 禁用头 | `<vector>/<string>/<memory>/<iostream>` — **零命中** |

### D8

| # | 锚点 | 命中 |
|---|------|------|
| a1 测试入口 | 875 TEST（kernel，65 文件）+ 49 host test |

### D10

| # | 锚点 | 命中 |
|---|------|------|
| a1 TODO/FIXME | 仅 3 处（boot.S:1 / signal.cpp:2） |

### D12

| # | 锚点 | 命中 |
|---|------|------|
| a1 最近 commit | 最近 10 全 `<type>(<scope>): 中文` 规范，无 Co-Auth |

## B 不变点判定

### D1（pass）

- [pass] **I1 C++17 无异常/无 RTTI**：throw/dynamic_cast/typeid 零命中（kernel 非 test）。
- [pass] **I2 ErrorOr 闭环 + syscall 翻 errno**：A 铁律（Q1-2 `[[nodiscard]]` 强化）。
- [pass] **I3 Cinux-Base 隔离**：kernel 不重写；禁用头零命中。
- [pass] **I4 依赖方向**：层化（arch→proc→syscall）。

**D1 = pass（架构铁律严格遵守）。**

### D8（warn，例外先读码）

- [pass] **I1 测试广度**：875 kernel test + 49 host test（F4 基线）。
- [warn] **I2 user-mode/SMP/设备盲区**：run-kernel-test 全 kernel-mode，不覆盖 user-mode segfault/demand（GOTCHA#11）；SMP 迁移/设备路径靠 `make run` GUI 冒烟（无断言）。已知。
- [pass] **I3 host mock 接口同步**：InodeOps/FDTable 签名同步（F-QA DEBT-017 坐实 mock 与真内核一致）。

### D10（pass）

- [pass] **I1 文档同步**：PLAN/debt/notes/GOTCHA 体系完整（Q2/Q3 4 报告 + handoff）。
- [pass] **I2 TODO 少**：仅 3 处（kernel 非 test）。
- [pass] **I3 注释 why + 设计记不做什么**：GOTCHA/notes 记踩坑与决策。

### D12（pass）

- [pass] **I1 一批一 commit 一验证**：8 commit 全绿。
- [pass] **I2 commit 规范**：`<type>(<scope>): 中文`，无 Co-Auth/AI 署名。
- [pass] **I3 一 commit 一域**：Q2/Q3/DEBT-017 分离。
- [pass] **I4 高危验证矩阵**：DEBT-017 用 host ASAN（Debug+Release+FORTIFY CI 对等）+ kernel 875/0（QUALITY-GATES §4）。

## 发现

无新 P0/P1。D1/D10/D12 全 pass；D8 warn 是已知盲区（GOTCHA#11，run-kernel-test 不覆盖 user-mode；非新债）。

## C 证据门槛

- D1 2 锚点 + D8 1 + D10 1 + D12 1 全跑命中进表。✅
- D1/D10/D12 全 pass；D8 warn（已知盲区）。逐条带证据。✅

## D 闭环

- 无新 DEBT。**Q3 收官 14/14 全审**（D1-D14）。
- 本报告：`reports/2026-06-21-d1-d8-d10-d12-audit.md`。
- **Q3 总结**（跨 4 批）：DEBT-002 精确坐实（P1 头号）+ DEBT-018（P2 kMaxCpus）/019（P3 用户指针）/020（P3 ELF 算术）新；D5/D7/D9/D1/D10/D12 pass（F4/FO/F-INFRA/架构清洁）；D8 warn（已知盲区）。喂 Q4（修 DEBT-002 头号 + DEBT-006 联动）。
